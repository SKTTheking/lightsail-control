# 安全检查记录

检查日期：2026-09-06。范围为本仓库的控制中心、Agent、固定的 Komari 构建基础及实际可达依赖。此记录不等于完整第三方审计，也不保证不存在未知漏洞或后门。

## 上游和构建来源

- Komari 官方仓库：https://github.com/komari-monitor/komari
- 固定版本 1.4.3，提交 `bf6b45ec3abfc56bba5e9223650a47a72f665371`。
- 本应用直接复用 `database/dbcore`、`database/models`、`database/clients`、`internal/metricstore`、`protocol/v1`。
- 自有入口为 `control/main.go`，构建目标 `./lightsail`。没有注册上游的通用 RPC2、插件、市场、上传、WebSocket 终端、OAuth 或公开监控路由，也不启动插件引擎。
- Agent 为本仓库可读的 `agent/agent.py`，通过 HTTPS 从自己的控制中心下载，并校验 SHA-256。没有自动更新、外部插件或隐藏监听端口。
- Go、Debian、Caddy 容器基础已核对 Docker 官方仓库，并固定多架构 manifest digest。固定 digest 保证不会悄悄漂移，不代表以后无需安全更新。
- 部署构建仍会访问 Go 模块代理/校验和服务、GitHub、Docker 官方仓库和 Debian/Ubuntu 包源；Go 依赖由 `dependencies/go.mod` / `go.sum` 固定，APT 包不做永久冻结。

## 已检查和加固的边界

| 检查项 | 处理 |
|---|---|
| 上游密码存储 | 1.4.3 的 `accounts.go` 使用固定盐 SHA-256。本入口不调用该密码校验；初始化使用 bcrypt cost 12，首次密码在部署机随机生成。 |
| 管理员认证 | 随机会话，服务端仅保存会话 token 哈希；8 小时有效，Cookie 为 HttpOnly / Secure / SameSite=Strict。 |
| 跨站请求 | 管理写操作同时验证准确 Origin 与独立 CSRF token；登录验证 Origin；不放开 CORS。 |
| 登录尝试 | 全局每分钟 10 次限制，避免通过伪造代理头绕过。该措施不是完整 DDoS 防护。 |
| Agent 身份 | 每台独立随机 bearer token，仅保存哈希；每次请求认证。不能调用管理员接口或修改其他服务器的任务。 |
| 接入代码 | 一次性 15 分钟令牌，原子消费；重新接入替换旧长期密钥；短代码不含永久 Agent token。 |
| 任务执行 | 明确 root 权限提示；仅管理员创建；脚本最长 64 KB、最长 7200 秒；一台服务器一个活动任务。 |
| 重试和重复执行 | 任务领取后不重新排队。事件序列去重。Agent 落盘执行日志，重启只上报中断，不重放脚本。 |
| 完成判定 | 必须先启动任务，再以退出码 0 上报成功。运行中进度最多 99%。服务本身是否可用仍由安装脚本验证。 |
| 断线与停止 | 连续回传失败约 60 秒后 Agent 杀死脚本进程组；中心约 120 秒无回传标记中断。用户可取消任务。 |
| 网页输出 | 日志、服务器名、结果均使用 textContent；不把服务端数据当 HTML。CSP 禁止内联脚本、外部脚本与被 iframe 嵌入。 |
| 链接 | 只接受 HTTP(S) 与列出的节点协议，拒绝 javascript/data 等；外链 noopener/noreferrer。 |
| 权限和端口 | 控制中心容器非 root、只读根文件系统、无 capabilities、无 Docker socket；只公开 Caddy 的 80/443。 |
| Agent 配置 | root-only 配置与状态，启动服务名称明确，无入站端口，不改 SSH；不关闭 TLS 校验、不跟随控制接口重定向。 |
| 输出大小 | HTTP 请求 256 KB 上限；任务日志服务端保留最后 1 MB；事件日志分批、内存缓冲有界。 |

## 依赖扫描结果

使用 `govulncheck v1.1.4` 扫描实际 `./lightsail` 构建目标：

- 初次 Go 1.25.7 构建：报告 17 个可达漏洞，涉及标准库和 `golang.org/x/text`。
- 修复：Go 升级到 **1.25.13**；`x/text` 升级到 **v0.39.0**；相应 `x/sync` 升级到 **v0.21.0**，锁文件一并保存。
- 再次扫描：**0 个当前调用路径可达的已知漏洞**。
- 扫描仍报告导入包中 9 个、依赖模块中 23 个未发现调用路径的告警。两个数字不能相加视作独立漏洞数。功能扩展后可能变为可达，更新依赖或引入新入口必须重扫。

这次扫描不包含 Python 解释器、宿主机内核、Docker Engine、容器 OS 包以及 Caddy 二进制的完整漏洞扫描。部署机系统补丁需要维护。

## 功能与安全验证

- Go 测试：未登录访问、CSRF 缺失、上游危险入口未注册、Agent 不能升级为管理员、不能访问其他节点任务、一次领取、接入令牌单次使用、旧密钥失效、事件不重复写日志、假成功拒绝、危险链接拒绝。
- Python 测试：脚本哈希检查、真实 Bash 成功/失败、进度和链接、恢复不重放、同月重启、网卡切换、UTC 跨月与链接过滤。
- 集成测试：启动真实编译后的控制中心，管理员登录、一次性接入、真实系统指标写入 Komari metrics.db、实际 Bash 脚本执行、进度/链接回传、失败退出码与撤销密钥。
- JavaScript 语法与 shell 语法检查。

## 剩余限制

1. 尚未在用户的 AWS 实例上部署，没有验证真实的 cloud-init、systemd、Caddy 自动证书、云防火墙和节点服务。测试环境没有 Docker daemon，未实际执行 Compose 启动。
2. 本应用为单管理员控制中心，尚无 MFA、多人权限管理或 AWS 账单 API 对账。
3. root Agent 是管理员明确安装的远程执行能力；如果管理员账号、目标服务器 root 或控制中心宿主机失陷，关联服务器可能被控制。它不是不可信脚本沙箱。
4. 拥有某台服务器 root 权限的人可伪造该服务器自己的指标和执行结果，无法仅靠 Agent 证明结果真实；但该密钥不能跨服务器或当管理员使用。
5. 任意 root 脚本可主动脱离进程组或修改 Agent；停止机制只约束正常脚本，不防恶意 root 脚本。中断不会回滚已经执行的操作。
6. 未对 Komari 全部源码逐行审计。当前检查没有确认存在恶意后门；这与“已证明不存在后门”不同。
7. 存储的脚本、日志、节点链接及备份可能含凭据；不要公开数据目录。任务和审计记录需要随使用量备份与维护，当前不自动删除历史任务。
8. 系统 root-only 初始密码文件会保留；不要提交、分享或误传。已初始化账号的密码不会随着该文件变化。

发现问题时，不要在公开 GitHub Issue 中贴密码、节点 URL、接入代码或未经脱敏的日志。
